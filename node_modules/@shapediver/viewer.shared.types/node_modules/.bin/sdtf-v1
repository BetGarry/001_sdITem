#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../sdk.sdtf-v1/bin/sdtf-v1.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../sdk.sdtf-v1/bin/sdtf-v1.js" "$@"
  ret=$?
fi
exit $ret
